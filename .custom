#!/usr/bin/env bash

# I use multiple SSH-keys, like it better that way. Add all of them
ssh-add ~/.ssh/id_rsa &>/dev/null;
ssh-add ~/.ssh/enrise_rsa &>/dev/null;
ssh-add ~/.ssh/github_rsa &>/dev/null;
